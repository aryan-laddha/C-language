#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n,f=1,i;
    printf("enter number: ");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        f=f*i;
        printf("factorial of %d is %d\n",i,f);
    }

    return 0;
}
