#include <stdio.h>
#include <stdlib.h>

int main()
{
int a[10],i;
printf("enter 10 number : \n");
for (i=0;i<10;i++)
{
scanf("%d",&a[i]);
}
printf("even numbers are : \n");
for (i=0;i<10;i++)
{
    if (a[i]%2==0)

    {
        printf("%d ",a[i]);
    }

}
}
